---
permalink: /concepts/
title: Concepts
---

<div class="max-w-4xl">
    <ol>
        {%- for post in collections.concept -%}
            <li>{% if post.data.tags contains "collection" %} <span class="font-inter">△</span> {% else %} <span
                        class="font-inter">○</span> {% endif %}
                <a
                        href="{{ post.url }}">{{ post.data.title }}</a></li>
        {%- endfor -%}
    </ol>
</div>
